package player;

import game_control.Board;
import game_control.PlayerMove;

public class AIFieldValue implements IPlayer {
    @Override
    public PlayerMove makeMove(Board currentBoardState) {
        return null;
    }
}
